# 工作区对齐（workspace-alignment）— TODO

**日期**: 2026-08-18 ｜ **规则**: plan.md 中 ❌/⏭️ 项的机械提取（Feature Pipeline）

## 延后项

| # | 项 | 状态 | 处置 |
|---|---|---|---|
| G-01 | 场景 3（多根变化）回归验证 | ⏭️ 受限 | 被"多 IDE 同一 `~/.dsh` 写冲突"阻塞（用户实测：仅同工作区+同会话冲突）→ **决策：接受为已知限制**，不根治共享 dsh（保持每 IDE 各管各的简单架构）；后续评估 |
| G-02 | 双 IDE 同工作区共享 `~/.dsh` 的写冲突根治 | ⏭️ | **决策：不根治**（共享 dsh 服务增加复杂度，非目标场景）；保持每 IDE 各管各的 dsh |

## 说明

- 本 feature 核心目标（R1 工作区切换 + R2 UI 对齐 + G-03 版本软校验）**已完成并验证**；延后项均为多 IDE 场景，不阻塞单 IDE / 不同工作区场景使用。
- G-03 版本软校验（含 `dsh.sessions.current` 内部键变更的兜底提示）已随本 feature 实现，不再列为延后项。

*关联：verification.md §5 ｜ summary.md 已知限制*
